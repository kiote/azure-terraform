{
    "name": "Azure Terraform Dev Container",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "features": {
        "ghcr.io/devcontainers/features/azure-cli:1": {}
    },
    "postCreateCommand": "terraform --version && ansible --version && az version"
}
